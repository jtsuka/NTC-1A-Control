# Claudeレビュー依頼 vendor_heatmap Ver0.6.1.8.2

固定の「検収日=納期の3日前 / 仕掛日=検収日の5日前」案内を廃止する微修正です。

## レビュー観点

1. 修正が固定案内削除に限定されていること。
2. `minInspectionToDueDays: 3` はリスク判定閾値として残っていること。
3. `key: 'ODERNO'` が維持され、前回修正を巻き戻していないこと。
4. ODERNO／注文書番号／旧Lookupフォールバック／入荷更新／負荷計算／リスク判定に変更がないこと。
5. 削除したJS関数・CONFIG項目・CSSクラスに参照残りがないこと。
6. JS構文に問題がないこと。
7. TEST → PROD の適用順序で問題ないこと。

## 自動確認

- `node --check`: PASS
- CSS波括弧対応: PASS
