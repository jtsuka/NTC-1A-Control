# 外注課ヒートマップ Ver0.6.2.0-TEST6
## App292 TEST仕様書 FINAL案
### ポップアップ編集 + 標準編集導線
### 2026-09-23

## 1. 位置づけ

Claude再レビュー結果:
- 方針 PASS
- Q5 STAFF_DD = フォーム設定APIを正本
- Q9 標準編集復帰 = sessionStorageフラグで1回強制再GET
- K暫定設定 = TEST前にフォーム定義で型確認
- E20 STAFF_DD取得失敗のFail Closed試験を追加

本書のE01-E35がApp292受入試験の基準。E21/E22は番号クリック起動導線の追加試験。

---

## 2. TEST前確認

### C01 STAFF_DD
`/k/v1/app/form/fields.json` の `STAFF_DD` 選択肢を正本として取得できる。

### C02 K暫定設定
フォーム設定APIで field code / type / options / PUT value形式を確認する。
型に応じてpayloadが正しいことを確認してからE系TESTへ進む。

### C03 TESTアプリ
App292で実施。App272 PRODへ直接投入しない。

### C04 受入れ日フィールド
App292に新規DATEフィールドを作成する。
- 表示名: `受入れ日`
- フィールドコード: `受入れ日`
- type: DATE
- required: false
- 初期値/今日自動設定: なし


---

## 3. E01-E20

E01: 日程編集ボタンで対象レコードのポップアップが開く。

E02: ポップアップ表示時、対象1件を再GETし最新値を表示する。

E03: K加工着手日のみ変更して保存すると、K日程種別がManualになる。
      未変更項目はPUTしない。

E04: K完成検査日のみ変更して保存すると、K日程種別がManualになる。

E05: K加工着手日 > K完成検査日は保存不可。エラー表示・入力保持。

E06: 片側日付のみ入力した状態を採用仕様どおり保存できる。

E07: K暫定設定 ON/OFF変更が実フィールド型に適したvalue形式で保存される。

E08: 手配担当者変更時、STAFF_TEXT / STAFF_DDが同じ値で同時更新される。

E09: 一覧フィルタで担当者が表示母集団から消えていても、
     フォーム設定に登録されたSTAFF_DD全候補がselectへ出る。

E10: revision競合時にPUT失敗し、自動上書きせず競合メッセージを表示する。

E11: 保存中の連打・キャンセル・クイックフィルタ操作で二重PUTや破棄が起きない。

E12: 入荷記録ポップアップと日程編集ポップアップが同時に開かない。

E13: 「その他の項目を標準画面で編集」で対象recordの標準編集画面へ遷移する。

E14: 未保存変更なしで標準編集へ移動した場合、余計な破棄確認なし。
     sessionStorage強制再取得フラグが設定される。

E15: 未保存変更ありで標準編集へ移動する場合、破棄確認が出る。
     キャンセルなら遷移しない。OK時だけ遷移。

E16: 標準編集で保存後、一覧へ戻るとlistQueryCondition同一でも強制再GET。
     最新値で再描画し、成功後にフラグ削除。

E17: 標準編集を保存せずキャンセルして一覧へ戻っても、強制再GETを1回行い、
     正しい元値を表示してフラグ削除。

E18: 強制再GETを人工的に失敗させる。
     エラー表示し、強制再GETフラグを残し、次回index.showで再試行可能。

E19: 既存機能回帰
     - 入荷記録ポップアップ
     - 外注先クイックフィルタ
     - kintone標準一覧絞り込み連動
     - Windows Shift+wheel横スクロール
     - Mac横スクロール
     - 今日マーカー
     - 表示サイズ 標準/大/特大
     - Manual表示/Mバッジ
     - 日程リスク
     - スクロール位置保持

E20: STAFF_DD取得失敗 Fail Closed
     フォーム設定APIを人工的に失敗させる。
     期待:
     - 日程編集ポップアップは開かない
     - エラーが明示される
     - state.records等の不完全な候補へフォールバックしない
     - 不完全な候補で編集を続行できない


E21: 手配書番号クリック起動
     - 手配書番号をクリックすると標準詳細画面ではなく日程編集ポップアップが開く
     - 正しいrecord IDで最新1件GETされる
     - ODERNOなしの「手配書番号なし」表示でもポップアップを開ける
     - 日程編集ボタンから開いた場合と内容が一致する

E22: 注文書番号クリック起動
     - 注文書番号が存在する行では、クリックで同じ日程編集ポップアップが開く
     - 手配書番号とは別番号でも同じrecord IDを使う
     - 注文書番号空欄（注文書 —）は非リンク
     - 日程編集ボタンから開いた場合と内容が一致する


E23: 外注入荷日 新規登録
     - 未入荷レコードでは日程編集ポップアップの外注入荷日は空欄
     - 有効な日付を入力して保存すると外注入荷実績日に保存される
     - 保存後、行が入荷済み表示へ変わる
     - revisionが更新される

E24: 外注入荷日 修正
     - 既存入荷日が初期表示される
     - 別日へ変更して保存できる
     - 既存の「入荷日修正」機能と同じ最終値になる

E25: 外注入荷日 取消
     - 既存入荷日を空欄へ変更して保存しようとすると確認ダイアログが出る
     - キャンセル時はPUTしない
     - OK時は `{ value: "" }` で空欄化し未入荷表示へ戻る
     - revisionが更新される

E26: 客先納期超過 / 既存入荷機能回帰
     - 外注入荷日 > 客先納期なら警告表示するが保存可能
     - 既存の「入荷しました / 入荷日修正 / 取り消し」も従来どおり動作する
     - 日程編集ポップアップと既存入荷ポップアップは同時に開かない


E27: 受入れ日 フォーム定義Fail Closed
     - App292にフィールドコード`受入れ日`、type=DATEが存在することを確認
     - フィールド不存在を人工的に再現するとポップアップを開かない
     - typeがDATE以外でもポップアップを開かない

E28: 受入れ日 新規登録・修正
     - 初期空欄からDATEを登録できる
     - 既存DATEを別日に修正できる
     - 変更時だけPUT対象になる
     - revision競合時は上書きしない

E29: 受入れ日 取消
     - 既存受入れ日を空欄にすると確認ダイアログ
     - キャンセルではPUTなし
     - OKで空文字PUTされ、受入れ日が空欄になる

E30: 外注入荷日との独立性
     - 外注入荷日だけ変更しても受入れ日は不変
     - 受入れ日だけ変更しても外注入荷日は不変
     - 両方変更した場合は同じrevisionの1回PUTにまとめられる
     - 現時点では日付前後関係を自動補正・拒否しない


E31: 日程リスク 手配書番号クリック起動
     - 手配書番号クリックで標準詳細画面へ遷移せず日程編集ポップアップが開く
     - クリック行のdata-record-idを使用する
     - 「手配書番号なし」でも開ける
     - 下段ガントから同じrecordを開いた内容と一致する

E32: 日程リスク 注文書番号クリック起動
     - 注文書番号ありはクリック可能
     - クリック行のdata-record-idでポップアップが開く
     - 同じ手配書番号が複数行でもクリックした行を取り違えない
     - 注文書番号空欄（—）は非リンク
     - 下段ガントから同じrecordを開いた内容と一致する

E33: 日程リスク回帰
     - 判定・理由・日付・最大30件・並び順に意図しない変化なし
     - リンクを開いただけではPUTしない
     - 保存時revisionガードを維持
     - 既存E01-E30に回帰なし


E34: ポップアップ 案件情報（表示のみ）
     - 手配書番号ありは正しい番号表示
     - 手配書番号なしは「手配書番号なし」と表示
     - 注文書番号ありは正しい番号表示
     - 注文書番号なしは「—」表示
     - 下段ガント / 日程リスク / 日程編集ボタンの各経路で同じ値を表示

E35: 番号フィールド表示専用保証
     - 手配書番号 / 注文書番号に入力UIがない
     - 番号表示だけではdirtyにならない
     - 保存payloadにODERNO / 数値_44が含まれない
     - K日程等を変更して保存しても番号系フィールドは不変

---

## 4. PASS条件

C01-C03 PASS
E01-E35 PASS
重大回帰 0件
revision競合時の上書き 0件
STAFF_TEXT/DD不一致 0件
標準編集復帰後の古いキャッシュ表示 0件
STAFF_DD API失敗時の不完全候補フォールバック 0件

全条件PASS後にのみApp272 PROD候補とする。
